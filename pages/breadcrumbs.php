<?php
	/* This page is kinda silly, but it's all because of how I implemented the whole templating 
	 * system. I can't really go back and change the whole thing, so this is my workaround. */

	print $objBreadcrumbs->get();

?>
